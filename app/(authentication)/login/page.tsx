import LoginPage from '@/components/authentication/login';
import React from 'react';

const Login = () => {
    return (
        <div > 
            <LoginPage/>
        </div>
    );
};

export default Login;